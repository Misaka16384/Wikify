# Modelfile for glm-ocr with increased context size
FROM glm-ocr:latest

# Increase context size for large images
PARAMETER num_ctx 16384
PARAMETER temperature 0

TEMPLATE {{ .Prompt }}
RENDERER glm-ocr
PARSER glm-ocr
