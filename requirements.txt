# Core dependencies
pyyaml>=6.0             # YAML parsing (llm-wiki.py, agent.py, add_concept.py, etc.)
filelock>=3.12.0        # Concurrent concept file writes (add_concept.py)

# PDF processing & OCR (pdf2md-agent)
requests>=2.28.0        # Ollama API calls (ocr_engine.py)
pillow>=9.0.0           # Image preprocessing (ocr_engine.py, pdf_processor.py, image_handler.py)
rich>=13.0.0            # Console output & progress bars (agent.py)

# Semantic linking (wiki_semantic_link)
scikit-learn>=1.0.0     # Cosine similarity (semantic_linker.py)
numpy>=1.20.0           # Vector operations (semantic_linker.py)

# Math Processing & Validation (validate_math_latex.py)
pylatexenc>=2.10        # Structural LaTeX AST parsing
