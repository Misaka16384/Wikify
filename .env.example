# Gemini Wiki Skills 环境变量配置
#
# 注意: 主要配置请编辑 config.yaml（由 install.ps1 部署到 .agents/config.yaml）。
# 此处的环境变量仅用于覆盖 config.yaml 中的对应设置（优先级更高）。
# 适用场景：CI/CD 环境、临时覆盖、多环境切换等。

# ========== 外部工具路径 ==========
# 覆盖 config.yaml 中 tools.* 的对应设置

# pdftoppm (poppler-utils) 路径
# PDFTOPPM_PATH=/usr/local/bin/pdftoppm

# pdfimages (poppler-utils) 路径
# PDFIMAGES_PATH=/usr/local/bin/pdfimages

# pandoc 路径
# PANDOC_PATH=/usr/local/bin/pandoc

# pandoc-crossref 路径
# PANDOC_CROSSREF_PATH=/usr/local/bin/pandoc-crossref

# ========== Ollama 配置 ==========
# 覆盖 config.yaml 中 ollama.base_url 的设置
# OLLAMA_HOST=http://localhost:11434

# ========== Hub 配置 ==========
# Hub 配置文件路径（默认: ~/.config/llm-wiki/config.json）
# LLM_WIKI_CONFIG=~/.config/llm-wiki/config.json
